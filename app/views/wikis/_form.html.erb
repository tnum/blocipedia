<%= form_for wiki do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :body %>
  <%= f.text_area :body %>

  <% if current_user.premium? %>
    <%= f.label 'Private wiki' %>
    <%= f.check_box :is_private %>
  <% end %>

  <%= f.submit "Save" %>

<% end %>